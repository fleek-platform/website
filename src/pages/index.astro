---
import Docs from './docs/index.astro';

const url = new URL(Astro.request.url);
const isResourcesDomain = url.hostname.startsWith('resources.');
const isHostingDomain = url.hostname.startsWith('hosting.');
const isElizaDomain = url.hostname.startsWith('eliza.');
---

{
    <Docs />
}
