import { NewsletterSubscribeModal } from '@fleek-platform/agents-ui';

export default () => (
  <>
    <h1>TODO: Remove this line (Newsletter test)</h1>
    <NewsletterSubscribeModal open={true} />
  </>
);
