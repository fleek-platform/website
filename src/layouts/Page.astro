---
import '@styles/globals.css';
import BaseHtml from './BaseHtml.astro';

interface Props {
  title: string;
  description: string;
  image: string;
  slug: string;
  singlePage?: boolean;
}

const { title, description, image, slug } = Astro.props;
---

<BaseHtml
  title={title}
  ogMeta={{
    title,
    description,
    image,
    slug,
  }}
  singlePage={Astro.props.singlePage}
>
  <main>
    <slot />
  </main>
</BaseHtml>
